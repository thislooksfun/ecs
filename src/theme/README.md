# Theme

This directory contains the base theming of items such as font, text color, and
button and input styles. These items should be reusable and backend-agnostic.
Namely, no using `context` or `fetch`, only wrappers around standard HTML
elements.
